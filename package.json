{
  "name": "serverless-color-converter-swd1tn002",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0",
    "test": "vitest run",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.9"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.4.0"
  }
}
